<% provide(:button_text, 'Create Book') %>

<div class='container px-5 px-sm-0'>
  <div class='row'>
    <div class='col-md-3'>
      <%= render 'users/user_info', user: @user %>
      <h2 class="mt-3">New book</h2>
      <%= render 'books/book_form', book: @book %>
    </div>
    <%= render 'books/books_list', bookslist: @users_books %>
  </div>
</div>